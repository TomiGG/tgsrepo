<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
<form id="login-form" method="POST" action="/users/login">
  {{#if errors}}
        {{#each errors}}
          <div class="alert alert-danger">{{msg}}</div>
        {{/each}}
  {{/if}}
  <h3 class="form-headings">Bitte einloggen!</h2>
  <div class="input-group spacing">
    <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
    <input id="email" type="text" class="form-control" name="username" placeholder="Username">
  </div>
  <div class="input-group spacing">
    <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
    <input id="password" type="password" class="form-control" name="password" placeholder="Password">
  </div>
  <div style="margin-bottom: 7px;">Noch keinen Account? <a href="/users/register">Hier registrieren!</a></div>
  <button type="submit" class="btn btn-default spacing" style="width:100%;">Submit</button>
</form>
